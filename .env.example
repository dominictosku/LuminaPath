# Copy this file to .env before running Docker Compose.

# Images
LUMINAPATH_API_IMAGE=luminapath-api:local
LUMINAPATH_FRONTEND_IMAGE=luminapath-frontend:local

# Public ports
BACKEND_HTTP_PORT=8080
FRONTEND_HTTP_PORT=4200
POSTGRES_PORT=5432
PGADMIN_HTTP_PORT=5050

# PostgreSQL
POSTGRES_VERSION=17-alpine
POSTGRES_DB=luminapath
POSTGRES_USER=luminapath
POSTGRES_PASSWORD=change-me

# ASP.NET backend
ASPNETCORE_ENVIRONMENT=Production
RUN_MIGRATIONS_ON_STARTUP=true
HTTPS_REDIRECT=false
FRONTEND_PUBLIC_URL=http://localhost:4200
IONIC_PUBLIC_URL=http://localhost:8100

# Storage
# FileSystem is the simplest self-hosted option. Azure is available for production cloud setups.
STORAGE_PROVIDER=FileSystem
STORAGE_PATH=/app/App_Data/storage
AZURE_BLOB_CONNECTION_STRING=
AZURE_BLOB_CONTAINER_NAME=media

# Angular Docker frontend
# /api means the Angular nginx container proxies API calls to the backend container.
LUMINAPATH_API_ENDPOINT=/api

# Optional pgAdmin
PGADMIN_VERSION=latest
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
